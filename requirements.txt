# Core
numpy>=1.24.0
soundfile>=0.12.0
sounddevice>=0.4.6
pyaudio>=0.2.14

# VAD
silero-vad>=5.0

# ASR
openai-whisper>=20231117
transformers>=4.38.0
torch>=2.1.0

# Translation
sentencepiece>=0.2.0
sacremoses>=0.1.1

# TTS (dependency stubs — actual models linked separately)
pydub>=0.25.1

# ONNX runtime (lightweight inference on CPU/edge)
onnxruntime>=1.17.0

# Config
PyYAML>=6.0

# Qualcomm AI Hub (for model export on Colab — not needed on edge device)
# qai-hub
# qai-hub-models
