# ── Core ASR ──────────────────────────────────────────────────────────────────
sherpa-onnx                        # GIPFormer ONNX runtime (VI ASR)
openai-whisper                     # Whisper (EN ASR, dùng bởi F5-TTS khi transcribe ref audio)
funasr_onnx                        # SenseVoice ONNX (EN/VI ASR + emotion detection)
modelscope                         # SenseVoice model loader

# ── Machine Translation ─────────────────────────────────────────────────
# EnViT5 SentencePiece tokenizer fails with Transformers 5.x; keep this measured Colab-compatible set.
transformers==4.57.1
tokenizers==0.22.1
sentencepiece==0.2.0               # Tokenizer cho VietAI/envit5 (T5 SentencePiece)

# ── Punctuation Restoration ───────────────────────────────────────────────────
deepmultilingualpunctuation        # kredor/punctuate-all (hỗ trợ tiếng Việt)

# ── TTS ───────────────────────────────────────────────────────────────────────
f5-tts                             # F5-TTS voice cloning (EN TTS — chính, chạy tốt trên Colab/Linux)
pyttsx3                            # Fallback TTS cho Windows (dùng Microsoft SAPI5, không cần cài thêm)

# ── Deep Learning ─────────────────────────────────────────────────────────────
torch
torchaudio
torchvision

# ── Audio Processing ──────────────────────────────────────────────────────────
soundfile                          # Đọc/ghi WAV
sounddevice                        # Phát audio real-time
librosa                            # Phân tích audio, time-stretch (speed control)
pydub                              # Chuyển đổi định dạng audio (mp3→wav)
pedalboard                         # Audio effects

# ── Infrastructure ────────────────────────────────────────────────────────────
huggingface_hub                    # Tải model từ HuggingFace Hub
PyYAML                             # Đọc config.yaml
numpy

# ── OmniVoice & Text Processing ───────────────────────────────────────────────
accelerate                         # HuggingFace Accelerate cho GPU inference
tqdm                               # Progress bars
unidecode                          # Xử lý text đa ngôn ngữ
cn2an                              # Text processing cho OmniVoice/F5-TTS
zhconv                             # Text processing cho OmniVoice/F5-TTS
zhon                               # Text processing cho OmniVoice/F5-TTS
webdataset                         # Data loader cho TTS
sacremoses                         # Tokenizer helper cho Translation/Punctuation
